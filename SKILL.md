---
name: llm-wiki
description: >
  LLM 驱动的个人知识库构建工具。触发场景：
  (1) /wiki:init 或「初始化知识库」「wiki init」——在当前项目目录初始化知识库结构；
  (2) /wiki:ingest 或「处理文章」「ingest」「摄入资料」「处理笔记」——将原始资料编译进知识库；
  (3) /wiki:consolidate 或「合并知识」「consolidate」「整理知识库」——合并 pending 内容为干净的 wiki 页面；
  (4) /wiki:query 或「查询知识库」「告诉我关于...」「wiki query」「知识库里有什么关于...」——从 wiki 检索并回答问题；
  (5) /wiki:lint 或「健康检查」「lint」「检查知识库」——对知识库进行健康检查，检测孤儿页、pending 堆积、缺失页面等问题。
  也响应单独出现的「知识库」关键词（当上下文涉及 wiki 操作时）。
---

# llm-wiki：编译型个人知识库

## 系统理念

这是一个「**编译型知识库**」，不是 RAG（检索增强生成）。

| 对比维度 | 传统 RAG | llm-wiki |
|----------|----------|----------|
| 知识形态 | 原始文档片段 | 预合成 wiki 页面 |
| 查询时工作 | 每次从原文推导 | 读已合成的 wiki |
| 知识积累 | 原文堆叠 | 持续编译、去重、综合 |
| 认知冲突 | 隐藏在原文中 | 显式 ⚡ 标注 |

**核心流程**：原始资料 → `/wiki:ingest`（追加 pending）→ `/wiki:consolidate`（编译成 wiki）→ `/wiki:query`（读 wiki 回答）

---

## 第一步：读取配置（每次运行任何子命令前执行）

1. 在**当前工作目录**查找 `wiki配置.json`
2. 若不存在且命令不是 `/wiki:init`，输出以下提示并**中止**：
   ```
   ❌ 未找到 wiki配置.json，请先运行 /wiki:init 初始化知识库
   ```
3. 解析路径字段，支持相对路径（相对于 `wiki配置.json` 所在目录）和绝对路径

**wiki配置.json 默认内容**：
```json
{
  "知识库路径": "./知识库",
  "原始资料路径": "./原始资料",
  "卡片路径": null,
  "卡片格式": null
}
```

---

## 子命令：/wiki:init

**触发词**：`/wiki:init`、`初始化知识库`、`wiki init`

### 执行步骤

**Step 1**：检查 `wiki配置.json` 是否已存在
- 若已存在，向用户确认：「检测到已有 wiki配置.json，是否重新初始化？（这不会删除现有知识库内容）」
- 用户确认后继续；用户取消则中止。

**Step 2**：创建目录结构（全部中文命名）

```
<当前目录>/
├── wiki配置.json
├── 原始资料/
│   ├── 待处理/        ← 用户放新文章
│   ├── 已处理/        ← ingest 后自动归档
│   └── 卡片/          ← 预留接口，当前跳过
└── 知识库/
    ├── 索引/
    │   ├── 概念索引.jsonl    ← 空文件
    │   ├── 实体索引.jsonl    ← 空文件
    │   └── 知识地图.md
    ├── 概念/
    ├── 实体/
    ├── 综合分析/
    └── 操作日志.md
```

**Step 3**：生成 `wiki配置.json`（使用默认值，如目录已存在则不覆盖）

**Step 4**：创建空的索引文件
- `概念索引.jsonl`：空文件（0 字节）
- `实体索引.jsonl`：空文件（0 字节）

**Step 5**：创建 `知识地图.md`（使用 `references/schema.md` 第四节的模板，填入初始数据）

**Step 6**：创建 `操作日志.md`，写入初始化记录：
```markdown
# 操作日志

### [YYYY-MM-DD HH:MM] /wiki:init

知识库已初始化。
```

**Step 7**：向用户展示完成摘要，说明目录结构和下一步操作建议。

---

## 子命令：/wiki:ingest

**触发词**：`/wiki:ingest`、`处理文章`、`ingest`、`摄入资料`、`处理笔记`

**执行方式**：读取 `references/ingest流程.md`，严格按其中的 Step 1 到 Step 9 执行。

> 本 SKILL.md 只做路由，完整执行逻辑在 `references/ingest流程.md` 中定义。

**快速摘要**（仅供 LLM 预览）：
1. 读取配置 → 2. 扫描 `待处理/` → 3. 过滤文件类型 → 4. 提取概念+实体 → 5. 索引匹配 → 6a. 新建页面 / 6b. 追加 pending → 7. 卡片接口插槽 → 8. 归档原文件 → 9. 写日志+统计

---

## 子命令：/wiki:consolidate

**触发词**：`/wiki:consolidate`、`合并知识`、`consolidate`、`整理知识库`

**执行方式**：读取 `references/consolidate流程.md`，严格按其中的 Step 1 到 Step 7 执行。

> 本 SKILL.md 只做路由，完整执行逻辑在 `references/consolidate流程.md` 中定义。

**快速摘要**（仅供 LLM 预览）：
1. 读取配置 → 2. 扫描有 pending 的页面 → 3. 空检查 → 4. 逐页合并（重写摘要、保留分歧、清空 pending） → 5. 检测重复页 → 6. 更新知识地图 → 7. 写日志

---

## 子命令：/wiki:lint

**触发词**：`/wiki:lint`、`健康检查`、`lint`、`检查知识库`

**执行方式**：读取 `references/lint流程.md`，严格按其中的 Step 1 到 Step 7 执行。

> 本 SKILL.md 只做路由，完整执行逻辑在 `references/lint流程.md` 中定义。

**快速摘要**（仅供 LLM 预览）：
1. 读取配置 → 2. 扫描所有 wiki 页面 → 3. 执行 5 项检查（孤儿页、pending 堆积、缺失页面、过时页面、交叉引用完整性） → 4. 输出分级报告 → 5. 写日志

---

## 子命令：/wiki:query

**触发词**：`/wiki:query`、`查询知识库`、`告诉我关于...`、`wiki query`、`知识库里有什么关于...`

### 执行步骤

**Step 1**：解析查询意图
- 识别用户想查询的概念名称或实体名称
- 若查询模糊（如「告诉我关于 attention 的内容」），提取关键词 `attention`

**Step 2**：扫描索引文件
- 读取 `<知识库路径>/索引/概念索引.jsonl`
- 读取 `<知识库路径>/索引/实体索引.jsonl`
- 对每条索引条目的 `标题` 和 `标签` 字段进行模糊匹配（规则同 ingest流程 Step 5）
- 收集所有匹配的条目（可能有多个）

**Step 3**：若无匹配结果
```
ℹ️ 知识库中未找到关于「[查询词]」的内容。
建议：将相关文章放入「原始资料/待处理/」目录，然后运行 /wiki:ingest
```

**Step 4**：读取匹配页面内容
- 读取所有匹配页面的文件（路径来自索引的 `文件` 字段）
- 分别提取 `📖 知识摘要` 区和 `📥 待合并内容` 区
- 提取页面中的所有 `⚡ 认知分歧` 块

**Step 5**：综合回答
- 基于 `📖 知识摘要` 区的内容回答用户问题
- 若 `📥 待合并内容` 区有内容，引用时加注：「（尚未合并，仅供参考）」
- **认知分歧必须主动浮出**，不埋在正文中，在回答末尾单独列出

**Step 6**：回答末尾附上参考信息
```markdown
---
**参考了 N 个 wiki 页面**：
- [页面标题](路径)（最后更新：YYYY-MM-DD）
- ...

[如有认知分歧]
**⚡ 注意：以下内容存在认知分歧**
- [概念名]：外部资料认为「[观点]」，来源：[文件名]
```

**Step 7**：回写 wiki（仅多概念综合回答时触发）
- 当回答涉及 **2 个或以上** 概念的综合分析时，询问用户：「本次综合分析是否保存到知识库？」
- 若用户确认保存：
  1. 生成综合分析页面（格式遵循 `references/schema.md` 第八节模板），保存到 `<知识库路径>/综合分析/<id>.md`
  2. 在 `知识地图.md` 的"最近活跃"表中追加记录
  3. 在操作日志中写入记录（格式：`### [YYYY-MM-DD HH:MM] /wiki:query | 查询「问题」，参考 N 个页面`）
- 若用户拒绝或回答仅涉及单个概念，跳过此步骤

**Step 8**：写入操作日志
- 无论是否回写 wiki，均在操作日志中追加查询记录
- 格式：`### [YYYY-MM-DD HH:MM] /wiki:query | 查询「问题」，参考 N 个页面`

---

## 全局约束规则

执行任何子命令时，以下规则始终有效：

1. **原始资料目录只读**：`原始资料/` 目录中的文件只允许「移动到 已处理/」，永远不修改文件内容

2. **卡片接口预留**：`卡片路径` 为 `null` 时，跳过所有卡片相关处理，**不报错**；在日志记录「卡片笔记接口未配置，已跳过」

3. **中文优先原则**：
   - 所有知识库产出**目录**必须使用中文命名（`概念/`、`实体/`、`综合分析/`、`索引/` 等）
   - 所有概念页、实体页的**标题**必须优先使用中文（英文专有名词如模型名、框架名、人名可保留原名）
   - 概念/实体的**标签**必须使用中文
   - **文件名（id）**：中文概念直接用中文词（如 `注意力机制.md`），英文专有名词保留英文（如 `transformer.md`），详见 `references/schema.md` 第六节

4. **格式规范遵从**：wiki 页面的创建和更新必须严格遵循 `references/schema.md` 中定义的模板

5. **认知分歧不可静默合并**：遇到认知冲突时，必须使用 `⚡ 认知分歧` 标注块显式记录，不得悄悄取一方观点

6. **📖 知识摘要区 ingest 时只读**：ingest 操作中，不允许修改 `📖 知识摘要` 区，只能追加到 `📥 待合并内容` 区

7. **日志只追加不覆盖**：`操作日志.md` 每次追加新记录，历史记录不删除、不修改。日志格式统一为 `### [YYYY-MM-DD HH:MM] action | summary`，详见 `references/schema.md` 第九节

8. **指令菜单规则**：每个子命令执行完毕后，在输出的**最末尾**固定附加指令菜单块，格式如下：

   ```
   ---
   📋 **可用指令**：
   - `/wiki:init` — 初始化知识库
   - `/wiki:ingest` — 摄入新资料
   - `/wiki:consolidate` — 合并待处理内容
   - `/wiki:query <问题>` — 查询知识库
   - `/wiki:lint` — 健康检查
   💡 **推荐下一步**：<根据上下文动态推荐>
   ```

   **推荐逻辑**：

   | 刚执行的命令 | 推荐下一步 |
   |---|---|
   | `/wiki:init` | `/wiki:ingest` — 将资料放入「原始资料/待处理/」后运行 |
   | `/wiki:ingest` | pending ≤ 10 → `/wiki:ingest`（继续添加资料）；pending > 10 → `/wiki:consolidate`（建议先合并积压内容） |
   | `/wiki:consolidate` | `/wiki:query`（知识已更新，可以查询了）或 `/wiki:ingest`（继续添加新资料） |
   | `/wiki:query` | `/wiki:ingest` — 添加更多资料以丰富知识库 |
   | `/wiki:lint` | 根据 lint 结果推荐（如有 pending 堆积 → `/wiki:consolidate`，有缺失页面 → `/wiki:ingest`） |

9. **跨 skill 协作提示**：当 `待处理/` 目录中发现不支持的文件格式时（非 .md、.txt），不直接跳过，而是向用户输出提示：

   ```
   ℹ️ 发现不支持的文件：<文件名>
   建议：使用对应工具先转换为 .md 格式再放入待处理/
   - .pdf 文件 → 可使用 pdf skill 提取文本
   - 图片文件 → 可使用支持视觉的 LLM 描述内容后保存为 .md
   ```

---

## 目录结构参考

```
用户项目/
├── wiki配置.json                 ← 配置文件（路径可自定义）
├── 原始资料/
│   ├── 待处理/                   ← 用户放新文章（.md .txt）
│   ├── 已处理/                   ← ingest 后归档（YYYYMMDD_原文件名）
│   └── 卡片/                     ← 预留接口，当前版本跳过
└── 知识库/                       ← 路径可在 wiki配置.json 自定义
    ├── 索引/
    │   ├── 概念索引.jsonl         ← 每行一条 JSON
    │   ├── 实体索引.jsonl         ← 每行一条 JSON
    │   └── 知识地图.md            ← 人类可读的全局概览
    ├── 概念/                      ← 概念 wiki 页面（如 transformer.md）
    ├── 实体/                      ← 实体 wiki 页面（人/书/工具/组织）
    ├── 综合分析/                  ← 预留，跨概念的深度分析文章
    └── 操作日志.md                ← 所有操作的历史追加记录
```

---

## References

- **页面格式规范**：`references/schema.md`（概念页/实体页模板、索引格式、日志格式、wikilink 规范、综合分析页模板）
- **Ingest 执行流程**：`references/ingest流程.md`（Step-by-step 完整指令）
- **Consolidate 执行流程**：`references/consolidate流程.md`（Step-by-step 完整指令）
- **Lint 执行流程**：`references/lint流程.md`（Step-by-step 完整指令）
